import { ServiceFormClient } from '@/components/admin/services/service-form-client'

export const metadata = { title: 'خدمة جديدة — مسار' }

export default function NewServicePage() {
  return <ServiceFormClient />
}
