# PHASE 5 — تقرير الأداء: تعمّق في إظهار المحتوى (MoBILE RENDERING + REVEAL PERFORMANCE DEEP DIVE)

التاريخ: 2026-08-16
البيئة: Playwright + CDP — 390×844 (وأيضاً 375/412/1440)، CPU throttle ×4، شبكة 4G-fast (60ms/3.7Mbps)
المنهج: Profile → Identify → Optimize → Measure → Regression → Verify

---

## 1) الجذر الفعلي للمشكلة (مقاس، لا تخمين)

قيسنا الخط الزمني الفعلي (عينات opacity كل 150ms + longtasks + LCP):

**Home قبل (390px):** FCP 1276–1760 | H1 ينجز عند ~1276 | لكن النص الوصفي (LCP النهائي) يبدأ الإظهار عند **3413** ويكتمل عند **3853–4008**.
**Services قبل:** FCP 868–920 | الوصف يبدأ **~3050** ويكتمل **3771** | LCP النهائي نص @**4028**.

السبب المباشر — سلسلة كاملة أثبتناها بصرياً على الـDOM:

1. **SSR يبعث المحتوى فوق الطية مخفيّاً:**
   - `motion` يطبع `initial` في HTML: `opacity:0` (services + عناصر home) و`translateY(105%)` داخل `overflow-hidden` للعنوان.
   - أي: البايتات النهائية موجودة منذ أول بايت، وتظهر **بعد** أن يبدأ JS.
2. **الإظهار معلّق على اكتمال التهيئة (hydration) وليس على الجاهزية:**
   - hydration يكتمل عند ~2.4–3.1s تحت ×4 (موجود في longtasks: خدمات حتى 2760، home حتى 3072).
   - عناصر home كانت ملفوفة أيضاً ببوابة إضافية: `mounted` (useState → useEffect) قبل أن ينطلق الـstagger أصلاً.
3. **بعد التهيئة: تأخيرات متراصة + قوائم انتظار:**
   - `delayChildren: 0.25` + `staggerChildren: 0.11` + مدة 0.6s (home)، وتأخيرات 0.2/0.35/0.65/0.9 + مدد 1–1.4s (services) — كلها **تُضاف فوق** زمن التهيئة لا بالتوازي معها.
   - كل frame يُصطف خلف longtasks أثناء التشغيل فيتمدد الجدول إلى 4+ ثوانٍ.

**ما لم يكن السبب (نفيناه بالأدلة):**
- الخطوط: جاهزة ~995ms (لا بوابات `document.fonts` — grep شامل صفر نتائج).
- intro/cinematic: `cinematic-intro` لا يقدم شيئاً لحالة "زار سابقاً" (`masar-intro-viewed`) — قياسنا يثبت غياب تأثيره.
- بوابات عامة (isLoaded/pageReady/introComplete): غير موجودة في الكود.
- إعداد "تقليل الحركة" في لوحة الإعدادات **غير موصول أصلاً** (لا أحد يقرؤه) — كل `useReducedMotion` من media query النظام.

**الاستنتاج القياسي:** المحتوى كان "جاهزاً" لكن الرؤية بانتظار hydration + تأخيرات JS + طوابير المعالج. التصحيح المستهدف: فك إظهار ما فوق الطية عن الـhydration كلياً.

---

## 2) التحسين المطبق (بدون تغيير لغة الحركة)

استبدلنا دخول كل عنصر حرج فوق الطية من `motion initial/animate + intervals` إلى **CSS animations بنفس المعاملات حرفياً** (نفس `cubic-bezier(0.22,1,0.36,1)`، نفس التأخيرات والمدد ومسافات الانزلاق، بما فيها clip-reveal للعنوان 105%):

| الملف | التغيير |
|---|---|
| `app/globals.css` | `@keyframes masar-reveal` + `masar-clip-up` + الصنفان `.reveal-hero`/`.reveal-hero-clip` مع متغيرات `--rv-delay/--rv-dur/--rv-y/--rv-scale` + معالجة `prefers-reduced-motion: reduce` (إظهار فوري بلا حركة) |
| `components/hero-section.tsx` | إزالة بوابة `mounted` + `STAGGER` variants؛ العنوان الفرعي/العنوان/الوصف/الأزرار/الإحصاءات → كشف CSS (تأخيرات 0.25/0.32/0.36/0.47/0.58s — مطابقة للجدول القديم) |
| `components/services/services-hero.tsx` | التسمية/العنوان/الوصف/الأزرار/إطار الصورة → كشف CSS (تأخيرات 0.2/0.35/0.65/0.9/0.2s — مطابقة)؛ تنظيف imports غير المستخدمة |

**لماذا آمن:**
- المعاملات مطابقة حرفياً — نفس المظهر، حتى لون العيONS الحركي لكل عنصر على حدة.
- لا حذف محتوى، لا إخفاء؛ التفاعلات (hover/tap/فتح الفيديو/الparallax/الجسيمات) باقية.
- الآن الحركة تبدأ من أول رسم بواسطة الـcompositor — **جاهز→إظهار** حرفياً.
- reduced-motion: كان في السابق لا يُحترم (إعداد التطبيق غير موصول) والآن يُحترم بشكل صحيح عبر media query.

---

## 3) القياسات قبل/بعد (نفس الأداة، نفس النمط ×4 CPU)

### Home

| المشارة | Before | After |
|---|---|---|
| FCP | 1276–1760ms | **1248–1412ms** |
| إظهار وصف الـhero (اكتمال opacity=1) | **3853–4008ms** | **≤ 2310ms** (opacity=1 عند أول عينة) |
| LCP | نص P @ 4008–4172ms | **H1 @ 1276–1404ms** |

### Services

| المشارة | Before | After |
|---|---|---|
| FCP | 868–920ms | **288–352ms** |
| إظهار الوصف (opacity=1) | ~3771ms | **≤ 2100ms** |
| إظهار الأزرار (opacity=1) | ~4062ms | **≤ 2700ms** |
| LCP | نص P @ 3580–4028ms | **IMG @ 564–656ms** |

### بقية المؤشرات (Home @390، ×4)

| المؤشر | Before | After |
|---|---|---|
| TBT (مجموع longtasks أول 6s) | ~0.9–1.2s | ~1.0–1.1s (متعادل؛ لا شيء أضفناه) |
| CLS | 0.0000 | **0.0000** (transforms فقط) |
| Longtasks مفردة | أقصاها 308ms | أقصاها 292ms |
| INP (نقرات حقيقية ×4) | — | home **584ms** / services **416ms** (تحت ×4؛ تقريباً ~150–200ms حقيقي) |

### المصفوفة الشاملة (After)

| العرض | home FCP/LCP | services FCP/LCP |
|---|---|---|
| 375px | 1300 / **1300 H1** | 312 / (انظر الملاحظة) |
| 390px | 1404 / **1404 H1** | 316 / **608 IMG** |
| 412px | 1276 / **1276 H1** | 1588 / **1856 IMG** |
| 1440px | 1360 / **1360 H1** | 352 / **656 IMG** |

أوضاع ar/en × dark/light @390 (After — home LCP): ar-dark **1404** | ar-light **1248** | en-dark **1332** | en-light **1412** — الكل H1؛ في الإنجليزية يظهر لاحقاً P/LCP نصي عند ~3428 (أثر font-swap بعد الإظهار المبكر فعلياً)، ما زال أقل من قبل.

**ملاحظة أدوات صريحة (375/services):** في عرض 375 بالذات لا يُصدر مؤشر LCP في عدّادنا؛ التحقق الميداني أثبت عرض صورة الـhero (naturalWidth=375، عُرضت 325) وopacity=1 للنصوص وFCP 312 — خلل إسناد وليس مشكلة مطبقة.

---

## 4) ما ينصّ عليه التقرير من تحقق إضافي

- **لا مشهد "انتظار→انتظار→إظهار":** الآن أعلى الصفحة = `ready → reveal` (من أول رسم).
- **LCP/Hero لا ينتظران أسفل الصفحة أو مواضيع غير مهمة:** إظهار الـhero مستقل بنيوياً (CSS) عن أي مكوّن آخر.
- **IntersectionObserver:** لا يُستخدم لإظهار الـhero (لم يكن أصلاً) — الفحوص الأخرى (whileInView أسفل الصفحة) لم تُلمس.
- **بنية فوق/تحت الطية:** تغييرنا يمس فقط العناصر الحرجة فوق الطية في صفحتين؛ بقية الموقع على نظامه الحركي.

---

## 5) Regression والتحقق الوظيفي

- **72/72 Regression PASS** (9 مسارات × ar/en × dark/light × 390/1440، بلا صور مكسورة ولا أخطاء JS ولا طلبات فاشلة).
- تبديل الثيم dark→light: يعمل (class `light`).
- RTL/LTR: مغطى في الـ72 (ar/en).
- التنقل الجوال: زر القائمة (40px) يفتح الدرج، روابط المشاريع/الخدمات/عن مسار ظاهرة، والتنقل إلى `/services` يعمل.
- Projects/details/gallery/360-viewer/contact/settings: مغطاة في الـ72 (كلها PASS).

---

## 6) الحالة الواضحة لكي لا يعاد سؤالها

- Intro السينمائي: محفوظ ومختصر فقط لأول زيارة (تصميم) — القياسات كلها لبيئة "زار سابقاً" كما في المراحل السابقة.
- إعداد "تقليل الحركة": **ليس** بوابة أداء؛ الآن CSS يحترم تفضيل النظام للمستخدمين الذين يفعلونه.
- صور CDN/مواضيع معروفة (hero 404، insights localhost، عدم وجود has360): خارج نطاق الأداء كما أقر النص.