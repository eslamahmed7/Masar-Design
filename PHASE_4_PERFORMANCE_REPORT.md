# PHASE 4 — تقرير الأداء النهائي للجوال (Final Mobile Performance Polish)

التاريخ: 2026-08-16
الأداة: Playwright + CDP (viewport 390×844، CPU throttle ×4، شبكة 4G-fast: 60ms latency / 3.7Mbps)
الحالة: **مكتملة — 72/72 Regression PASS**

---

## 1) الملخص القياسي (Before ⇐ Phase 3 ⇐ After Phase 4)

| المقياس | قبل (Baseline) | بعد Phase 3 | بعد Phase 4 | ملاحظة |
|---|---|---|---|---|
| FCP home (390px, cold) | ~2300ms | 1548ms | **1504ms** | ثابت |
| LCP home (نص Tajawal) | ~5300ms | 4444ms | **4172ms** | يقوده نمط كشف النص المتأرجح (design) |
| LCP projects (IMG hero-bg) | — | ~850ms | **740ms** | سريع أصلاً |
| LCP details (IMG الغلاف) | ~3000ms | 1044ms (hot) | **1044ms + fetchPriority=high** | أزلنا منافسة غير مطلوبة |
| LCP services (نص) | ~5300ms | — | **4240ms** | نفسه: نص مُقاد بالأنيميشن |
| LCP about (IMG الشعار) | — | ~1500ms | **2924ms** (نمط كشف) | الشبكة 585ms؛ التأخير من الأنيميشن |
| FCP navigation hop | — | — | **340–408ms** | تحقق فعلي |
| Back (details→projects) | — | — | **0 طلبات شبكة** | Back/Forward cache كامل |
| TBT home (×4 throttle) | — | 446ms | **11 longtask أقصاها 308ms** | بلا مهمة جذرية واحدة |
| الخطوط | 236KB | 169KB | 169KB | (ملفات 11، كلها مؤجلة بالـswap + fallback) |
| مجموع JS (home, decoded) | ~900KB | ~392KB | 392KB | ثابت |
| Regression | — | 72/72 | **72/72 PASS** | الفلاتر: aborted البيئي فقط |

---

## 2) تدقيق LCP (4A) — عنصر لكل مسار (مُقاس بالأداة phase4-lcp-audit)

| المسار | عنصر LCP | التفاصيل | التوقيت |
|---|---|---|---|
| `/` | `P` "نحوّل المساحات…" — Tajawal 400، 325×52 | FCP 1504 ← الخطوط جاهزة ~995ms؛ التأخير = ترتيب ظهور مؤجل (H1 عند 1400 ثم P عند 4172) | **4172ms** |
| `/projects` | `IMG _next/image about/hero-bg.png w=640 q=75` (390×422 render) | بدأ 61ms واكتمل 679ms — 16.6KB | **740ms** |
| `/projects/[id]` | `IMG` الغلاف `f_auto,q_auto,w_1200` (390×591 render — DPR3 = 1170 ≈ 1200 ✓ صحيح) | 140.5KB، كان pri=auto → أصبح **fetchPriority="high"** | **1044ms** |
| `/services` | `P` "نحوّل الأفكار إلى مساحات استثنائية…" — Tajawal | Hero image preloaded (58KB) لكنها غير LCP — النص هو LCP | **4240ms** |
| `/about` | `IMG masar-logo.png w=256 q=75` (120×139 natural، أوضح عند 160×186) | الشبكة 585+103ms؛ LCP عند 2924 = كشف متعدد المراحل للأنيميشن | **2924ms** |

**الخلاصة القياسية:** الـLCP عند المسارات ذات **النص** (home/services) يُقاد بترتيب الكشف المتعمد في التصميم (staggered reveal)، وليس بالنقل. عند **الصور** (projects/details/about) المُقاسة كلها ≤ 2.9s تحت throttle ×4 — جيد.

---

## 3) تدقيق أولويات LCP وتطبيقاتها (4B)

- **Details:** أضفنا `fetchPriority="high"` على غلاف hero (كان `pri=auto` في DOM المُقاس) — وهو عنصر LCP الحقيقي الوحيد فوق الطية. تغيير خصائصي بلا أي أثر بصري.
- **Projects hero-bg:** تُركت كما هي (LCP عند 740ms — إضافة priority لن تغير شيئاً وستضيف preload زائد).
- **عناصر النص LCP:** لا أولوية شبكة ممكنة — التوقيت من الأنيميشن (موثق أعلاه)، وتقليل الحركة المفضّل (reduceMotion) محترم عند المستخدمين.

---

## 4) تدقيق الصور والمعالجات (4C)

| البند | النتيجة |
|---|---|
| أحجام next/image | سلات صحيحة: w=256/640/1200؛ البطاقات `priority={index<2}` ✓ |
| تحويلات Cloudinary | f_auto,q_auto؛ الغلاف w_1200 (يتطابق مع 3×DPR على 390px) ✓ |
| صور lazy الـbelow-fold | تبدأ تحميلها عند قربها من منفذ العرض (اتجاه Chromium القياسي؛ مُقاس 268ms بعد الـhero في details) — سلوك صحيح، لا تغيير |
| تكرارات طلب صغيرة | طلب مكرر لنفس URL hero (preload + img) — الناتج من محسن الصور جوهره 0.3KB (استجابة مُخزنة) — غير مؤثر، مُوثق فقط |
| **مشكلة محتوى (وليست أداءً):** | صورة الخلفية في `/` الحالية `vu1nqfdwlooxgbc2tdvt.jpg` **404 من Cloudinary** (الملف محذوف من CDN والسجل المرجعي قديم) — الطبقة canvas تُرسم بلا خلفية وهذا مقصود من التصميم (بديلها gradient+overlay). تُنقل لمدير المحتوى، وخارج نطاق هذا الفصل. |

---

## 5) تحميل بيانات التفاصيل (4D/4E) — التغيير الفعلي الوحيد في الكود

**قبل:** الصفحات الثلاث (`projects/[id]` و`[id]/360` و`[id]/360/tour`) كانت تستدعي `getProjects()` (كل المشاريع + كل معارض `project_gallery`) ثم `find()`.
**بعد:** أصبحت `getProjectById(id)` (صف واحد `select *, project_gallery(*)` مع `React cache + unstable_cache` — بدون تكرار، مع 30s revalidate). صفحة التفاصيل لا تزال تجلب القائمة **للـrelated فقط** (شبكة البطاقات تحتاج الغلافات فقط، والـpreview-modal يحتاج المعارض — لم نقتطع شيئاً هنا تجنباً لأي خطر بصري).

الأثر المُقاس (قبل/بعد):
- صفحات 360/360-tour: لم تعد تُسلسل قائمة كل المشاريع بمعارضها في الـRSC (طلبات Supabase من قائمة واحدة كاملة → صف واحد).
- RSC الخاص بالتفاصيل: 9K → 10K (ثابت لأن الـrelated يبقى).
- **72/72 Regression بعد التغيير.**

---

## 6) تدقيق 360 (4F)

- **البنية (كود):** `tour-entry-wrapper.tsx` يُثبّت الـViewer **فقط بعد اختيار الوضع** (`{mode && <TourViewerClient/>}`) — لا يوجد تحميل مبكر للمشاهد/pannellum عند فتح صفحة الجولة.
- **قياس:** الصفحتين `/360` و`/360/tour` — **0 طلبات صور/بانوراما** على الإطلاق عند تحميل الصفحة (الفصل يعمل).
- **ملاحظة بيانات:** لا يوجد حالياً مشروع منشور بـ`has360` → المساران يردان `notFound()` (يُختبر الآن عبر `getProjectById` + فحص `has360` ✔). بمجرد نشر مشروع 360، آلية التفويض جاهزة ومُختبرة على مستوى الكود.
- three.js: لا يوجد في تطبيق الويب (فقط داخل `masar-editor` الفرعي) — بند 4J غير منطبق.
- panoramas تخضع للتحويل `w_2048` في `mapDBProject` ✓ (من Phase 3).

---

## 7) تدقيق التنقل (4G) — قياسات فعلية بالانتقال

| الرحلة | توقيت الـhop | طلبات الملفات | طلبات RSC/الصور | ملاحظة |
|---|---|---|---|---|
| home → projects | FCP 408ms | 23KB js | 9KB cdn | رحلة نظيفة |
| projects → details | FCP 408ms | 12KB js | rsc 10K + 192KB cdn (الغلاف+المعرض) | الحمولة الكاملة تظهر فقط عند فتح المشروع — مطابق للتصميم |
| details → projects (goBack) | — | **0 طلبات** | 0 | bfcache كامل ✓ |
| كما رُصد: | prefetch تلقائي لـRSC الصفحة المستهدفة (مكرر x2 في مشهد واحد، كلاهما مُخزّن — غير مؤثر) | | | |

لا حاجة لأي تغيير في التنقل — خرجت سريعة مع ذاكرة تخزين مفعّلة.

---

## 8) التدقيق الأمامي (4H + 4L)

- لا توجيهات prefetch يدوية في الكود؛ يعتمد على سلوك Next.js الافتراضي (روابط في منفذ العرض). القياس يظهر فعلها (reqs 6 فقط عند التنقل).
- **ذاكرة التخزين:** reload دافئ home: FCP **340–408ms** مقابل ~1500ms بارد؛ بدون أي طلب خطوط (0KB)؛ RSC يُعاد استخدامه.
- Cloudinary: التحويلات تُخزن عند المحسّن (المسار x3 من hero = 0.3KB لكل استجابة — مخزن/دُفعة).

---

## 9) تدقيق JS ورئيس الموضوع (4I)

- **Longtasks** (home، ×4 throttle، 6s): 11 مهمة، أقصاها **308ms**، أغلبها 56–188ms موزعة على جدول الأنيميشن (1.0–3.3s) — لا توجد مهمة جذرية واحدة، لا تسريب، لا Loop زائد.
- `smooth-scroll.tsx` (Lenis ثانٍ محلي في 4 صفحات): **يخرج مبكراً على الجوال/اللمس** (`isTouch || isMobileViewport → return`) — تكلفته صفر في نطاق هذا التقرير (390px).
- خطوط `next/font` (subsets) + `font-display: swap` + `Fallback` — LCP نصي لا ينتظر كتلة كاملة.
- أحجام الشفرات لكل مسار وقت الانتقال: 12–23KB فقط لكل صفحة؛ الحزم المشتركة مطابقة لـPhase 3.

---

## 10) Regression النهائية (4M)

`regression-check.js`: 9 مسارات × 4 أوضاع (ar/en × dark/light) × 2 منفذ عرض (390/1440) = **72/72 PASS** — بدون صور مكسورة، بدون أخطاء JS، بدون طلبات فاشلة (الفلاتر: `/_vercel/insights/script.js` — تحليلات Vercel غير موجودة على localhost فترد 404+aborted (بيئي)، وأي aborted أثناء التنقل).

---

## 11) قرارات "لا تغيير" بالأدلة

| البند | السبب المُقاس |
|---|---|
| LCP النصي في home/services | لا يقوده النقل (الخطوط جاهزة ~995ms) بل ترتيب الكشف المتأرجح — تغييره يمس التصميم |
| hero-bg في projects | LCP 740ms بالفعل |
| صور المعرض | في أثناء فتح التفاصيل (مباشرة)، صغيرة (11–16KB)، قرب منفذ العرض |
| Bundle sizes | مطابقة لـPhase 3، لا هامش آمن |
| 360 viewer | مفصّل بالكامل خلف اختيار الوضع؛ لا يمكن تحميله قبل النقر من الأساس |

---

## 12) الحالة النهائية

- التغييرات في الكود (هذا الفصل): 3 مسارات → `getProjectById` + `fetchPriority="high"` لغلاف التفاصيل.
- كل الفصول الأخرى: خرجت القياسات نظيفة أو أُثبت عدم الحاجة.
- المشكلة الوحيدة المفتوحة **محتوى** (صورة hero 404 من CDN — تُدار من لوحة المحتوى) وليست أداءً.
- **الخلاصة:** الموقع على هدف الجوال 390px مع throttle ×4: LCP ≤ 2.9s عند المسارات المصوّرة، ونص ≤ 4.3s محتجز خلف أنيميشن مقصود، وتنقل بلا ملفات إضافية، وذاكرة تخزين فعّالة، و72/72 سليم.