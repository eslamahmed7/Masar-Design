# تقرير فحص الجودة — Masar Design (مسار ديزاين)

**التاريخ:** 15 أغسطس 2026
**النطاق:** QA شامل + إصلاحات تلقائية + اختبار Regression + لقطات شاشة
**الهدف:** نسخة Production-Ready سريعة ومتقنة على الجوال والكمبيوتر، مع الحفاظ الكامل على الهوية البصرية.

---

## 1) بيئة الاختبار

| البند | القيمة |
|-------|--------|
| الإطار | Next.js 16.2.6 (Turbopack) + React 19 + Tailwind CSS v4 |
| الخادم | Dev server محلي على `http://localhost:3000` |
| قاعدة البيانات | Supabase (PostgREST) |
| الصور | Cloudinary |
| الأداة | Playwright + Chrome headless (`C:\Program Files\Google\Chrome\Application\chrome.exe`) |
| المقاسات | موبايل 390×844 (×2) · موبايل صغير 375×812 (×2) · تابلت 768×1024 · كمبيوتر 1440×900 |
| اللغة | ar-EG (RTL) + اختبار تحويل EN/LTR |

## 2) تغطية الاختبار

- **11 مسارًا:** `/` `/projects` `/projects/[id]` `/projects/[id]/360` `/projects/[id]/360/tour` `/services` `/about` `/contact` `/start` `/admin/login` `/admin` + صفحة 404
- **36 حالة HTTP** (9 صفحات × 4 مقاسات): كلها `200`، وصفحة 404 المتعمّدة `404`
- **21 اختبارًا تفاعليًا** على المتصفح: القائمة، التنقل، الفلاتر، البحث (Ctrl+K)، لوحة الإعدادات (لغة/ثيم)، Login اللوحة، مسار `/start`
- **فحوصات تلقائية:** تجاوز أفقي، صور مكسورة، أخطاء Console، طلبات فاشلة، تحذيرات `next/image` و LCP

## 3) النتائج النهائية

| الفحص | النتيجة |
|-------|---------|
| HTTP status لجميع الصفحات/الأجهزة | ✅ 200 (عدا 404 المتعمد) |
| Horizontal overflow | ✅ صفر على جميع المقاسات |
| صور مكسورة في الصفحات | ✅ صفر |
| تحذيرات Console | ✅ صفر (بعد الإصلاحات) |
| الاختبارات التفاعلية | ✅ 21/21 ناجح |
| تسلسل إرسال فورم التواصل | ⚠️ يفشل على الـDB — يحتاج تدخلًا يدويًا (انظر البند 5) |

## 4) المشاكل المكتشفة والإصلاحات المطبقة

| # | المشكلة | الخطورة | الإصلاح | الملفات |
|---|---------|---------|---------|---------|
| 1 | طلب `global_promotions` يستخدم `.single()` بلا نتائج → خطأ HTTP 406 على **كل صفحة** | عالية (ضجيج + Console error لكل زيارة) | التحويل إلى `.maybeSingle()` | `components/global/promotion-banner.tsx` |
| 2 | صورة الـHero مكسورة (رابط Cloudinary محذوف → 404) وتُعرض كفراغ أسود | عالية (واجهة الصفحة الرئيسية) | بديل تلقائي: عند فشل التحميل يظهر الـplaceholder السينمائي الموجود مسبقًا في التصميم | `components/hero-section.tsx` |
| 3 | زر "تحميل المزيد" في معرض المشاريع **بلا أي `onClick`** (زر ميت يظهر دائمًا) | متوسطة (وظيفة مكسورة) | تنفيذ pagination حقيقي: عرض 8 مشاريع، الزر يظهر فقط عند وجود المزيد، ويُخفى عند عرض الكل | `components/projects-page-client.tsx` |
| 4 | عدم وجود زر الإعدادات (اللغة/الثيم) في هيدر **الموبايل** نهائيًا | متوسطة (إعدادات الموقع غير متاحة على الجوال) | إضافة زر الإعدادات بجانب زر البحث في الهيدر الموبايل | `components/luxury-header.tsx` |
| 5 | تحذير `next/image`: الأصول العائمة (`/objects/*`) بـ`fill` داخل أب `position: static` | منخفضة (تحذير أداء) | إضافة `relative` | `components/cta-section.tsx` |
| 6 | تحذير `next/image`: صور الخدمات من Cloudinary بـ`fill` داخل أب `static` | منخفضة | إضافة `relative` | `components/service-card.tsx` |
| 7 | تحذير `next/image`: شعار `/masar-logo.png` في مقدمة صفحة "عن" — ارتفاعه يتغير (185px) بدون عرض | منخفضة | رسم الصورة بصندوق ثابت 160×186 مع `fill` | `components/about/logo-intro.tsx` |
| 8 | تحذير LCP: `/about/philosophy.png` بدون `loading="eager"` | منخفضة (أداء) | إضافة `priority` | `components/about/about-philosophy.tsx` |
| 9 | تحذير Next 16: ملف `middleware.ts` مهجور (Deprecated) | منخفضة (استقبالية) | إعادة التسمية إلى `proxy.ts` مع تصدير `proxy` (مطابق لمواصفات Next 16) | `proxy.ts` |
| 10 | تحسين وصولية: الـCommand Palette بلا `role="dialog"` | منخفضة (a11y) | إضافة `role="dialog"` + `aria-modal` | `components/global/command-palette.tsx` |

## 5) مشاكل متبقية — تتطلب وصولًا إلى قاعدة البيانات (معلّقة)

| # | المشكلة | السبب | الحل المطلوب |
|---|---------|-------|--------------|
| 1 | **إرسال فورم التواصل يفشل**: `new row violates row-level security policy for table "contact_messages"` | الـPolicy المطلوبة موجودة في الملف `supabase/migrations/018_contact_faq.sql` لكنها **غير مفعلة في قاعدة البيانات الحي** (لا يوجد service_role key ولا supabase CLI هنا) | تطبيق هذا الـSQL على قاعدة Supabase يدويًا: <br> `CREATE POLICY "Public can insert contact_messages" ON contact_messages FOR INSERT WITH CHECK (true);` <br> (أو من لوحة Supabase: SQL Editor → تشغيل الملف) |
| 2 | طلب شبكة 404 واحد في Console عند تحميل الرئيسية (صورة الـHero المكسورة) | قيمة `image_url` في جدول `hero_settings` تشير لملف محذوف من Cloudinary | رفع صورة Hero جديدة من **لوحة التحكم → إعدادات الواجهة → الـHero** (لا يوجد وصول كتابة للـDB بالمفتاح الحالي) — البديل المرئي التلقائي يعمل بالفعل في الأثناء |

## 6) ملاحظات إضافية

- صفحة `/contact` تستخدم نموذج التواصل البسيط `ContactCommsForm` (المتحقق من البنية) — نموذج الخطوات المتعددة `contact-form.tsx` **غير مستخدم** حاليًا في أي صفحة.
- `/auth/callback` مسار POST فقط؛ طلب GET يعيد 404 وهذا سلوك طبيعي.
- فحص "لا يوجد حلقة لا نهائية": خلال 30 ثانية لا يصدر إلا عدد طبيعي من طلبات Supabase.
- قائمة المشاريع الحالية في قاعدة البيانات: **مشروع واحد منشور** فقط (بدون 360°) — ولهذا لا تظهر نتائج في تبويب 360° حتى تُضاف مشاريع جديدة، وهو سلوك صحيح.
- في الزحف الأول كانت صفحة تفاصيل مشروع على 375px تأخذ >90 ثانية مرة واحدة — لم تتكرر في الجولات اللاحقة (تسخين أولي).

## 7) اللقطات

مجلد `screenshots/` داخل المشروع يحتوي 36 لقطة كاملة (9 صفحات × 4 مقاسات) + `README.md` يشرح المحتوى وطريقة إعادة الالتقاط.

## 8) الحالة النهائية

**جاهز للإنتاج** بعد إتمام بندين من البند 5 (تطبيق الـSQL الخاص بـ`contact_messages` واستبدال صورة الـHero من لوحة التحكم). كل ما عداهما مُصلَح ومُختبَر.
