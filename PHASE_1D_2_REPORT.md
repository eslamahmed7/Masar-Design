# تقرير المرحلة PHASE_1D_2 — التحقق الشامل من i18n (AR/EN) + RTL/LTR عبر 4 وضعيات

**التاريخ:** 15 أغسطس 2026
**النطاق:** اكتمال ترجمة جميع الواجهات العامة (incl. مسار 360، الخدمات، المشاريع) ثم اختبار مصفوفة 4 وضعيات (AR/EN × dark/light) على موبايل 375 وكمبيوتر 1440
**الهدف:** ضمان أن كل واجهة عامة تُعرض باللغتين مع انعكاس RTL/LTR صحيح، دون overflow أو انكسار تصميم، مع بقاء تبديل الإعدادات حيًّا ومستمرًّا

---

## 1) ملخص النتائج

| الفحص | النتيجة |
|-------|---------|
| اختبار العرض الشامل (9 صفحات × 4 وضعيات × منفذان) | ✅ **280/280** |
| اختبار التبديل الحي (لغة/ثيم) + dir + persistence (7 صفحات) | ✅ **49/49** |
| Horizontal overflow | ✅ صفر (كل الصفحات/الوضعيات/المنافذ) |
| أخطاء Console / PageErrors | ✅ صفر |
| ESLint على كل الملفات الملموسة | ✅ 0 أخطاء (تحذير واحد سابق غير متعلق) |
| tsc | ✅ لا أخطاء جديدة (الـ5 المسبقة فقط، خارج النطاق) |

## 2) بيئة الاختبار

| البند | القيمة |
|-------|--------|
| الخادم | Next.js dev على `http://localhost:3000` (سجل `dev-server.log`) |
| الأداة | Playwright + Chrome headless |
| المقاسات | 375×812 (mobile) · 1440×900 (desktop) |
| الوضعيات | AR/dark · EN/dark · AR/light · EN/light (عبر `masar_settings` في localStorage، مع `masar-intro-viewed=true`) |
| الصفحات | `/` `/projects` `/projects/[id]` `/projects/[id]/360` `/projects/[id]/360/tour` `/services` `/about` `/contact` `/start` |

## 3) تغطية الاختبار

### أ) العرض واللغات (280 فحصًا)
- **Overflow**: `documentElement.scrollWidth <= innerWidth` لكل صفحة/وضعية/منفذ + تتبّع العناصر المسببة (صفر مخالفات).
- **السمة**: class `dark`/`light` على `<html>` مطابق للإعداد.
- **اللغة**: نص شريط التنقل الظاهر («أعمالنا»/«Our Work») على الكمبيوتر مباشرة، وعلى الجوال عبر فتح قائمة الجوال والتحقق من روابط الدرج الظاهرة فعليًّا (``getBoundingClientRect + display/visibility``).
- **مسار 360**:
  - صفحة العرض `/360`: `dir` صحيح، زر «تجربة 360°» / «360° Experience» موجود.
  - العارض `/360/tour`: واجهة اختيار الوضع «اختر طريقة الاستكشاف» + «جولة موجهة/استكشاف حر» ظاهرة باللغتين، والنقر على «ابدأ الاستكشاف» يكشف الغرف («غرفة المعيشة»/«Living Room») في اللوح الجانبي، وبدون أخطاء Console.
- **مسار `/start`**: زر «التالي»/«Next» أو CTA الأول موجود في الوضعيات الأربع.

### ب) التبديل الحي + الاتجاه + الاستمرارية (49 فحصًا)
لكل صفحة: فتح لوحة الإعدادات → تبديل الإنجليزية (يتحول `dir` من `rtl` إلى `ltr` + `lang=en` + نص التنقل إنجليزيًا **دون إعادة تحميل**) → تبديل الثيم الفاتح (تظهر class `light`) → ثم في سياق جديد بلا حقن: كتابة `{theme:light, language:en}` وإعادة التحميل → تبقى `ltr + light + lang=en + تنقل إنجليزي`.

## 4) إصلاحات أُجريت خلال التحقق

| # | المشكلة | الخطورة | الإصلاح | الملف |
|---|---------|---------|---------|-------|
| 1 | خطأ Runtime على **كل** الصفحات: `useSettings must be inside SettingsProvider` — `LightboxProvider` كان يرسم خارج `SettingsProvider` في layout بينما يستدعي `useI18n` | عالية (الموقع لا يعمل) | نقل `LightboxProvider` إلى داخل `SettingsProvider` | `app/layout.tsx` |
| 2 | أزرار الهيدر: زر القائمة على الجوال هو الزر الثالث (بعد الإعدادات والبحث) — اكتشاف اختباري زائد عن الحاجة، لا تغيير مطلوب في الكود | — | (توثيق فقط) | `components/luxury-header.tsx` |

## 5) إعداد بيانات اختبار مؤقتة (ثُم حُذفت)

قاعدة البيانات تحتوي مشروعًا واحدًا منشورًا **بدون** 360 (`has_360=false`)، ومسارات 360 تستدعي `notFound()` عند غيابها، والـRLS يمنع الإدراج بالمفتاح العام. لذلك:
- أُضيف مؤقتًا **مشروع تجريبي** (`qa-tour-0000-...`) بداخله غرفتان + hotspots بصور Cloudinary demo، خلف متغيّر بيئة `QA_TOUR_TEST=1` في `lib/projects.ts`.
- أُجريت كل اختبارات العارض 360 عليه، ثم **حُذف الكتلة نهائيًا** وأعيد تشغيل الخادم نظيفًا (التحقق: مسار المشروع التجريبي يعيد 404 الآن، والرئيسية 200).

## 6) ملاحظات فنية (نتائج التحقق)

- صفحة العارض `/360/tour` لا تعرض الهيدر (fullscreen مقصود) — فحص التنقل يُسقط لها تلقائيًّا.
- على الجوال، التنقل في درج مغلق (مخفي عن `innerText`) — الفحص الصحيح عبر فتح القائمة؛ النتائج أظهرت الدرج بكامل الروابط باللغتين.
- مقدمة `CinematicIntro` تغطي الصفحة لحظيًّا عند الزيارة الأولى (زر Skip) — الاختبارات تخطتها عبر `masar-intro-viewed`.

## 7) متبقٍّ خارج النطاق (موثّق كقرارات)

| البند | السبب |
|-------|-------|
| واجهات admin (`app/admin/**`) و`masar-editor` | لوحة تحكم داخلية، لا تخضع لتبديل اللغة العام |
| الملفات الميتة: `site-header.tsx`، `contact-form.tsx`/`service-selection.tsx`، `contract-pdf-link.tsx` | غير مستوردة في أي مسار فعلي |
| محتوى مستند PDF التعاقدي (`contract-pdf-button`) | وثيقة قانونية عربية مقصودة (زر التحميل نفسه مُعرَّب) |
| أخطاء tsc المسبقة: `pannellum` TS2687 + مشاكل bigint/ReactPortal في `masar-editor` | غير متعلقة بهذه المرحلة |